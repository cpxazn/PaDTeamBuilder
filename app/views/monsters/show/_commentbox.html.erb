
<div class="clear-left">
<%= form_tag({controller: "comments", action: "create", params: {leader_id: @leader.id, type: @type, sub_id: @sub.id}}) do  %>
  <div>
	<%= text_area_tag(:comment_text, nil,size: "60x5") %>
	<br>
	<%= submit_tag("Create Comment") %>
  </div>
<% end %>
</div>